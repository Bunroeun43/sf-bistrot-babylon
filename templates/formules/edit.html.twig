{% extends 'base.html.twig' %}

{% block title %}Edit Formules{% endblock %}

{% block body %}
<div class="page">
    <h1>Edit Formules</h1>

    {{ include('formules/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_formules_index') }}">back to list</a>

    {{ include('formules/_delete_form.html.twig') }}
</div>
{% endblock %}
