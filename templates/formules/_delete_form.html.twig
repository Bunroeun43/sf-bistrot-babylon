<form method="post" action="{{ path('app_formules_delete', {'id': formules.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formules.id) }}">
    <button class="btn">Delete</button>
</form>
