<form method="post" action="{{ path('app_desserts_delete', {'id': dessert.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dessert.id) }}">
    <button class="btn">Delete</button>
</form>
